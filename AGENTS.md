# 有奖竞猜项目规则

## 执行入口

1. 足球预测、体彩核对、版本、赛果、统计及复盘开始前，完整读取`football_rules.md`。预测细则以该文件为唯一正文；第七节规定两项输出与每日两场精选，第九、十三节规定版本与统计，第十六节规定术数停用。
2. 正式预测前读取`data/prediction_history.json`、`data/review_rules.json`及`data/review_rule_index.json`。索引归并共同原则，不替代原规则的联赛、赛制、强弱、让球与触发条件；先核对源文件校验码，失效时刷新索引。
3. 当前存储职责见`data/storage_policy.json`：JSON是有效记录源，SQLite尚未落地。旧JSON的数据库名称为保留的历史元数据。
4. 新记录以`data_structure_template.json`为字段基准，赛前模板为同结构单轮副本；映射、状态和校验入口见`templates/字段与校验说明.md`。新正式版本须通过`scripts/validate_prediction.py`；旧记录保留原schema，不追溯补造。
5. 旧导出目录的构建脚本为当时版本归档，不作为新入口，不为适配新模板重跑。
6. 后续预测另读`项目规则/赛前执行检查_2026-09-17复盘.md`及`data/review_rules.json`中的`execution_review_batches`，按联赛、赛制、强弱、官方让球和触发证据匹配后执行；这是待验证场景检查，不自动变更总规则或概率。

## 数据与版本保护

1. 总规则只保存用户确认的长期规则；预测、赛果及复盘写入历史，场景经验写入规则库，不能混写。
2. 冻结赛前字段、历史四项预测及原版本全部保留；新规则只适用于后续记录。事实更新、纠错、用户重新评估按总规则第九节独立留档。
3. 索引合并文本，不删除原ID、场景、证据和应用历史，不把不同联赛直接合并计数；缺失历史条件、概率及样本数保留未知。
4. `项目规则/交接基线_2026-09-04/`及历次修改前备份仅供追溯，未经明确要求不修改、覆盖、重命名或删除。
5. 修改活动规则、模板或索引前保留可恢复副本；交付说明改动和验证。

## 执行优先级

用户当前明确指令 > 本文件 > `football_rules.md` > 匹配当前场景的历史复盘规则 > 一般经验。索引、模板、历史备份不能改变优先级。

2026-09-17用户确认实施规则梳理建议，备份及变更记录见`项目规则/2026-09-17_规则优化实施/`。之后新增长期规则仍须用户明确确认。
